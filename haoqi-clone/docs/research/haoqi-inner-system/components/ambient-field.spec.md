# Ambient field

Fixed canvas behind the article. Draws a sparse, low-opacity dot matrix whose displacement and opacity respond to the pointer. It contains no route content, never intercepts input, tracks device pixel ratio and disables continuous movement when reduced motion is requested.
