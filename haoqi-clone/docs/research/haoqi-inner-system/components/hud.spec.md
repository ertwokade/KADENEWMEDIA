# HUD

Fixed transparent overlay, full viewport, pointer events disabled by default. Interactive children re-enable pointer events. Top controls use uppercase Geist Mono at 13px; the brand uses TikTok Sans at 17px. Desktop gutters are 4vw and collapse to 14px on mobile.
