"use client";
import { useState } from "react";
export default function LoginPage() { const [password, setPassword] = useState(""); const [error, setError] = useState(""); async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ password }) }); const body = await response.json(); if (!response.ok) setError(body.error); else location.href = "/"; } return <main id="main" className="login-page"><form onSubmit={submit}><p className="eyebrow">INTERNAL ACCESS</p><h1>Kurgu masasına<br/><em>giriş yap.</em></h1><label>STÜDYO ŞİFRESİ<input type="password" autoFocus required value={password} onChange={(event) => setPassword(event.target.value)} /></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="primary-button full">GİRİŞ →</button></form></main>; }
